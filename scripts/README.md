# V3 diagnostic scripts

From the project root:

```bash
python scripts/check_ollama.py
```

This checks the configured V3 model roles, connects to the local Ollama API, and reports installed models.
